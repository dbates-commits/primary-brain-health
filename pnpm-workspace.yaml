packages:
  - "apps/*"
  - "packages/*"

# Native deps whose install/build scripts are allowed to run.
# (pnpm 10 blocks postinstall scripts by default.)
onlyBuiltDependencies:
  - better-sqlite3
  - esbuild
  - sharp
  - unrs-resolver
  - core-js
