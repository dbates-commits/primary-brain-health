{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "ignoreCommand": "git diff --quiet HEAD^ HEAD -- ':/pnpm-lock.yaml' && npx turbo-ignore funnel",
  "buildCommand": "if [ \"$VERCEL_ENV\" = \"production\" ]; then pnpm --filter @pbh/db run db:migrate; fi && pnpm run build"
}
